# Security settings for API directory

# Prevent direct access to config and helper files (they are only included)
<FilesMatch "^(config|common)\.php$">
    Order Allow,Deny
    Deny from all
</FilesMatch>

# Prevent access to .env file
<Files ".env">
    Order Allow,Deny
    Deny from all
</Files>

# Prevent access to .env.example
<Files ".env.example">
    Order Allow,Deny
    Deny from all
</Files>

# Enable error logging (optional, for debugging)
php_flag display_errors off
php_flag log_errors on

# Set maximum execution time
php_value max_execution_time 30

# ============================================
# OPÇÃO: Definir variáveis de ambiente via .htaccess
# ============================================
# Descomente as linhas abaixo e substitua pelos seus valores
# Nota: Nem todos os servidores permitem SetEnv em .htaccess

# SetEnv YOUTUBE_API_KEY "sua_api_key_aqui"
# SetEnv INVIDIOUS_INSTANCE "https://invidious.f5.si"
# SetEnv MAX_RESULTS "20"
# SetEnv APP_ENV "production"
